{
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.17.2"
  },
  "scripts": {
    "start": "cd app && node server.js",
    "setup": "psql -U bekele -f setup.sql",
    "test": "pytest test_vehicle.py -v"
  }
}
